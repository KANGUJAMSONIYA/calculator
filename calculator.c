#include<stdio.h>
main()
{  int sum=0,diff=0,a,b,c,d;
	printf("enter the nos to be added:");
	scanf("%d%d",&a,&b);
	sum=a+b;
	printf("The sum of two nos is: ",sum);
	printf("enter the nos to be subtract:");
	scanf("%d%d",&c,&d);
	diff=c-d;
	printf("diff of two nos is: ",diff);
}
